def concat(str1, str2):
    """
    Concatenates two strings with a comma and a space between them.
    
    Args:
        str1 (str): The first string.
        str2 (str): The second string.
    
    Returns:
        str: The concatenated string in the format "str1, str2".
    """
    return f"{str1}, {str2}"
